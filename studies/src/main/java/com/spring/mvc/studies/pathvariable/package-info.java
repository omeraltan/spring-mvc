package com.spring.mvc.studies.pathvariable;

/**
 *
 *      @PathVariable
 *      This annotation can be used to handle template variables in the request URI mapping, and set them as method parameters.
 *
 *      1. A simple use case of the @PathVariable annotation to extract the templated part of the URI,
 *         represented by the variable {id}
 *      2. If the path variable name is different, we can specify it in the argument of the @PathVariable annotation.
 */
